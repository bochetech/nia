# Root pytest configuration
[pytest]
asyncio_mode = auto
testpaths =
    services/model-adapter/tests
    services/tenant-manager/tests
    services/orchestrator/tests
    services/rag-service/tests
addopts = -v --tb=short
filterwarnings =
    ignore::DeprecationWarning
